# Используем официальный образ Amazon Corretto 17 в качестве базового образа
FROM amazoncorretto:17

# Указываем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем готовый JAR-файл приложения из локальной папки target в контейнер
COPY target/*.jar app.jar

# Указываем порт, который будет прослушиваться контейнером
EXPOSE 8761

# Определяем команду запуска приложения
ENTRYPOINT ["java", "-jar", "app.jar"]